node_modules/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

# Secrets — .env holds live Twilio credentials
.env
.env.*
!.env.example

# Prisma generated client
apps/api/generated/
apps/api/src/generated/

# Local tooling
.DS_Store
*.log
.pnpm-store/
.turbo/
playwright-report/
test-results/

# Project-local Node toolchain (see .tools/env.sh). Binaries, never committed.
.tools/node/

# Playwright MCP scratch output — accessibility snapshots and screenshots written during
# browser testing. Session artefacts, never source.
.playwright-mcp/

# Screenshots dropped at the repo root by browser tooling. Deliberately narrow rather than
# a blanket *.png, so a real image can still be committed into docs/.
/*.png
